node_modules/
out/
build/
# Certyfikat Authenticode (lokalnie / CI — nie commituj). Self-signed skrypty usuniete; tu zostaje miejsce na PFX od wydawcy.
build/codesign.pfx
build/codesign-password.txt
.vite/
dist/
*.log
.DS_Store
.env
.env.*
# Klucz API modów (tylko lokalnie / na dysku użytkownika — nie commituj)
launcher-mods-api-key
branding/launcher-mods-key
Thumbs.db
asar.txt
app-asar-new.txt

# Opcjonalny exe (często duży / licencja) — odkomentuj jeśli nie chcesz go w repo
# launcher/*.exe

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
